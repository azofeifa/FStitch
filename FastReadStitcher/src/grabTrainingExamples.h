#ifndef grabTrainingExamples_H
#define grabTrainingExamples_H
#include "read.h"
#include <map>
#include <string>
using namespace std;
// class run_out{
// public:
// 	vector< vector<double> > X ;
// 	vector<int> Y;
// 	bool EXIT;
// 	run_out(vector< vector<double> >, vector<int> y);
// 	run_out();
// };
// map<string,T> makeIntervalTree(map<string, interval *>);
// run_out run_grabTrainingExamples(map<string,T>, map<string,contig *>, bool);

#endif